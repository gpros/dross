// Non-secret configuration. Both values are safe to commit to a public repo.
// Fill these in after you deploy the Apps Script Web App and create the OAuth client.
// See the README for the exact steps.

// The Apps Script Web App /exec URL (Deploy > New deployment > Web app).
export const API_URL = "https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec";

// The OAuth 2.0 Client ID (Web application) from Google Cloud Console.
// This is public by design — it is not a secret.
export const GOOGLE_CLIENT_ID = "REPLACE_WITH_CLIENT_ID.apps.googleusercontent.com";
